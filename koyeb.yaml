name: sammy-po
services:
  - type: web
    name: sammy-po-web
    runtime: docker
    dockerfile: Dockerfile
    ports:
      - port: 3000
        http: "/"
    env:
      - key: NODE_ENV
        value: production
